name: Build APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
          cache: true

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            /opt/hostedtoolcache/Flutter/3.24.0-stable/android-sdk/linux/
          key: ${{ runner.os }}-flutter-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-flutter-

      - name: Install dependencies
        run: |
          flutter pub get

      - name: Create assets directories
        run: |
          mkdir -p assets/images
          mkdir -p assets/audio
          mkdir -p assets/fonts
          touch assets/images/.gitkeep
          touch assets/audio/.gitkeep
          touch assets/fonts/.gitkeep

      - name: Flutter info
        run: |
          flutter --version
          flutter doctor -v

      - name: Flutter analyze
        run: |
          flutter analyze --no-fatal-infos --no-fatal-warnings || true

      - name: Build debug APK
        id: debug_build
        run: |
          set -x
          flutter pub get
          flutter build apk --debug 2>&1 | tee debug_build.log
          EXIT_CODE=${PIPESTATUS[0]}
          echo "=== EXIT CODE: $EXIT_CODE ===" >> debug_build.log
          echo "=== Debug APK files ===" >> debug_build.log
          find build -name "*.apk" -type f -exec ls -lh {} \; >> debug_build.log 2>&1 || true
          cat debug_build.log
          exit $EXIT_CODE

      - name: Upload debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: tap-cockroach-debug
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 14

      - name: Save debug build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-build-log
          path: debug_build.log
          retention-days: 3

      - name: Build release APK
        id: release_build
        run: |
          set -x
          flutter build apk --release 2>&1 | tee release_build.log
          EXIT_CODE=${PIPESTATUS[0]}
          echo "=== EXIT CODE: $EXIT_CODE ===" >> release_build.log
          echo "=== Release APK files ===" >> release_build.log
          find build -name "*.apk" -type f -exec ls -lh {} \; >> release_build.log 2>&1 || true
          cat release_build.log
          exit $EXIT_CODE

      - name: Upload release APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tap-cockroach-release
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: Save release build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-build-log
          path: release_build.log
          retention-days: 3